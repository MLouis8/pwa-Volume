import React from 'react'
import { ExcelDisplay } from '../../components/excelDisplay'

// These functions are only responsible for displaying the excel database

// We will see in the future if it's necessary to allow modifications in vehicles and locations from the user
// maybe they will be able to do it in the new trip page directly
// So the parameters is only composed of view functions and informations about how to use the app

export function locations() {

}

export function vehicles() {

}